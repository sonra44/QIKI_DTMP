# ORION: известные пробелы/вопросы (по фактам) — 2026-01-20

Источник: `docs/design/operator_console/ORION_OS_VALIDATION_CHECKLIST.md` (текущие ✅/❌ пометки).

## Events workflow

- Пока не закрыто оператором “по картинке”:
  - поведение `Unread/Непрочитано` в паузе (видимость в always-visible chrome + очистка `R`);
  - `X` (clear acknowledged) — ожидание: реально удаляет acked инциденты из таблицы;
  - выбор строки `↑/↓` должен обновлять инспектор (детерминированно).

## Resize / tmux устойчивость

- Есть ❌ пункт про ресайз: при split 60/120 строки переносятся и “плывёт” структура.
  - Цель: вместо wrap — трим с `…`, без разрушения chrome.

