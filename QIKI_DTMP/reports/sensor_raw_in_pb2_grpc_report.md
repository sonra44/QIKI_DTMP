# Отчёт: generated/sensor_raw_in_pb2_grpc.py

## Назначение
Проверка совместимости gRPC-версий для сообщений `sensor_raw_in`.

## Задачи
- Сравнивает установленную версию `grpcio` с требуемой `1.74.0`.
- Выбрасывает `RuntimeError` при несовместимости версий.

## Комментарии
Файл не содержит классов клиента или сервера, так как исходный `sensor_raw_in.proto` не определяет сервисов.
