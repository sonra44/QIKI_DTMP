# Отчёт: generated/sensor_raw_in_pb2.py

## Назначение
Сгенерированный модуль Protobuf с определением сообщения `SensorReading`.

## Задачи
- Описывает структуру `SensorReading` с полями идентификаторa, типа сенсора и временной метки.
- Поддерживает различные форматы данных (`vector_data`, `scalar_data`, `binary_data`) через механизм `oneof`.
- Импортирует вспомогательные типы из `common_types_pb2` и `timestamp_pb2`.

## Комментарии
Файл генерируется автоматически; изменения следует вносить в исходный `sensor_raw_in.proto`.
