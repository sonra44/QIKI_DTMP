# Анализ minimal_component.proto.template

## Задачи
1. Определить имя пакета через `component_name`.
2. Указать конкретное имя сообщения вместо `{{ component_proto_type }}`.
3. При необходимости добавить дополнительные поля помимо `id` и `name`.
4. Расширить импорты и комментарии, если компонент требует большего описания.
5. Добавить завершающую новую строку в конец файла.
