# TASK: Аудит этапа 8 — залипание cap-гейта, клип чипа, усиление пинов

**ID:** TASK_20260709_STAGE8_AUDIT_FIXES
**Status:** in_progress
**Owner:** Claude (CLI-агент), срез task-0052 (запрос оператора: «проверка этапа 8, поиск ошибок и исправление»)
**Date created:** 2026-07-09

## Goal

Адверсариальный аудит этапа 8 (`55ca76a..fce0137`) двумя субагентами
(код с Pilot-прогонами + мутационный тест-аудит), верификация глазами,
фиксы подтверждённого.

## Operator Scenario (visible outcome)

- Кто выполняет: operator
- Что честнее: cap-гейт чипа PWR больше НЕ залипает — просевший суперкап
  (▸БУСТ → ▸СТАБ) виден сразу, даже когда battery SoC не менялся (кэш-ключ
  ре-рендера не видел anchor_text — доказано Pilot-прогоном аудитора:
  «cap 10% STAB» рисовался как «cap 80% BUST»); на узком экране (160 кол)
  клип чипа съедает таймер «~мин», а не безопасность-гейт (сегмент стал
  компактным «cap78%▸БУСТ» и идёт ПЕРЕД таймером); физически невозможный
  SoC (вне 0..100) даёт честное отсутствие гейта, не «СТАБ».

## Reproduction Command

```bash
bash scripts/prove_orion_v_stage8.sh
```

## Before / After

- Before: кэш-ключ `status_bars._refresh` без anchor_text → cap-гейт
  обновлялся только со сменой battery SoC (HIGH — ложная готовность к
  пику); сегмент « · cap 78% ▸БУСТ» — последний в чипе и на 160-кол
  терминале клипался целиком (смок жил на 200-кол); classify_cap_gate(-5)
  → «stab»; префилл палитры затирал набранный черновик F5; упавший
  push_screen залипал quit-guard навсегда; палитра не закрывала открытый
  командный режим (рассинхрон с Input-путём). Тесты: AST-пин освобождал
  расширения команд («help detail» пряталось за «help»); лимит 24 симв.;
  «не unknown» без следа зеленел бы на мёртвой корутинной ветке; hardcode
  «cap 70%» переживал все тесты; help-тест довольствовался заголовками.
- After: anchor_text в кэш-ключе + Pilot-регресс «смена только cap
  обновляет лейбл»; компакт-формат до таймера + пин порядка; guard 0..100;
  префилл только в пустое поле; guard снимается при исключении;
  `_palette_run_typed` закрывает командный режим (кроме F5/И1); AST-пин —
  точное членство, лимит 40; поведенческий пин требует СЛЕД (синк/help/
  история); процент cap сверяется на 45/10; help-тест требует конкретные
  команды + полнота всех имён реестра.

## Impact Metric

- Baseline: HIGH-залипание воспроизведено Pilot'ом; 4 мутации выживали.
- Target/Actual: **29 passed** (пакет этапа 8 + новые регрессы); все
  мутационные дыры закрыты; оба тест-скоупа 0 FAILED; prove **EXIT=0**
  живьём («cap81%▸БУСТ ~102м» — новый порядок, quit-модал, мир STOPPED);
  ruff-дифф чист (2 E501 pre-existing).

## Scope / Non-goals — карта находок

- [x] HIGH F1: кэш-ключ чипа + anchor_text (+Pilot-регресс).
- [x] MED F2: компакт cap-сегмент перед таймером (клип узкого экрана
  съедает минуты, не гейт) + пин порядка.
- [x] LOW F3: classify_cap_gate вне 0..100 → None (+тест).
- [x] LOW F4: `_palette_run_typed` — паритет с Input-путём.
- [x] LOW F5: префилл не затирает непустой ввод.
- [x] LOW F6: quit-guard снимается при упавшем push_screen.
- [x] Тест-дыры A/B/D/E/F/G (мутационный аудит) — закрыты.
- Не взято (LOW C, зафиксировано): литерал с ':' в середине невидим
  AST-пину — узкий класс, помечено в докстринге пина.
- Чисто по аудиту: экстракция роутера построчно эквивалентна (только
  quit→confirm); подмена Quit-записи валидна (только «Quit» матчится);
  F4-экран показывает все 16 строк help (deque 200, рендер [-24:]);
  «(прод.)»-разрез без потерь (42/42 команды); quit-guard мутация
  ловится; смок supercap устойчив (продюсер всегда отдаёт float).

## Canon links

- Priority board (canonical): `~/MEMORI/ACTIVE_TASKS_QIKI_DTMP.md`
- Related: `TASKS/TASK_20260709_stage8_command_surface.md` (этап 8)

## Plan (steps)

1) 2 адверсариальных субагента + верификация глазами. [сделано]
2) Фиксы кода (HIGH+MED+4 LOW) и тестов (6 дыр) + регрессы. [сделано]
3) Досье + гейты + коммит + main + рестарт консоли + чекпоинт. [этот шаг]

## Definition of Done (DoD)

- [x] Docker-first checks passed (commands + outputs recorded)
- [x] Docs updated per `DOCUMENTATION_UPDATE_PROTOCOL.md` (это досье)
- [x] Операционный сценарий воспроизводится по `Reproduction Command`
- [x] Есть измеримый `Impact Metric` (baseline -> actual)
- [ ] Repo clean — после коммита среза

## Evidence

```
[smoke] чип PWR живьём: ▸ PWR OK 80% cap81%▸БУСТ ~102м ✓   ← гейт перед таймером
[smoke] quit → модал → n: консоль жива, guard снят ✓
[smoke] Этап 8 PASS: командная поверхность честна на живом стеке
```
Пакет 29 passed; tests/unit и src-деревья 0 FAILED.
