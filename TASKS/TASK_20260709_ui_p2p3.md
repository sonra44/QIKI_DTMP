# TASK: UI P2/P3 — семантика рейла, палитра стрипа, дедуп MFD-CSS, якоря F2, русские кнопки

**ID:** TASK_20260709_UI_P2P3
**Status:** in_progress
**Owner:** Claude (CLI-агент), срез task-0046 (второй UI-проход после P1-пакета 0043)
**Date created:** 2026-07-09

## Goal

Закрыть UI-хвосты P2/P3 пост-ревью. Детальные формулировки первого ревью
утеряны при компакте контекста — вместо гадания проведена СВЕЖАЯ верификация
субагентом по коду: 6 ярлыков CONFIRMED, 1 NOT-FOUND, +3 бонусных находки
(главная — B1: регрессия русского пульта до английского).

## Operator Scenario (visible outcome)

- Кто выполняет: operator
- Что видно: сегменты ACTION RAIL несут семантический цвет (CMD failed —
  красный, P>0/ACT required/INC — янтарный, M LIVE — зелёный) вместо единой
  одноцветной строки; стрип КОНТУР МИССИИ красится палитрой пульта (та же
  семантика, что у ui_rich), а не терминальными green/yellow/red/cyan;
  кнопки панели остаются русскими «Панель ▲/▼» после первого же refresh
  (была деградация в «Panel ▲/▼»); на F2 рамки MFD получили титулы-якоря
  («ЛЕВЫЙ MFD · <страница>» и подписи «страницы: [ / ]») — раньше кромка
  была покрашена под титул, но пустовала; стрелки едины: ← → (навигация)
  и ▲ ▼ (панели), ASCII `<-`/`->` ушли; правый MFD-экран получил
  зеркальный отступ (был перекошен в сторону кнопок).
- Под капотом: ~140 строк дублированного MFD-CSS двух экранов схлопнуты в
  одного владельца `orion_v.tcss` (селекторы те же, по id — поведение
  тождественно, дальнейшие правки в одном месте).

## Reproduction Command

```bash
docker exec qiki-dev-phase1 python -m pytest tests/unit/test_ui_p2p3.py tests/unit/test_orion_v_header.py tests/unit/test_orion_v_action_bar.py -q
```

## Before / After

- Before: рейл `#orionv-help` собирался `" | ".join(...)` в один Text без
  спанов; header.py держал 5 словарей ANSI-имён; `_refresh_playable_loop_buttons`
  перезаписывал compose-лейблы английскими литералами (два владельца);
  systems.py CSS красил `border-title-color` трёх рамок, но титулы не
  ставились; MFD-CSS был скопирован между cockpit.py и systems.py и уже
  начал расходиться; правый экран наследовал `margin: 0 1 0 0` от общего
  правила; три вокабуляра стрелок (`▲▼`, `<-`/`->`, `<`/`>`).
- After: `build_action_rail_text(loop)` — чистая функция, plain Text
  (урок a58fd97 сохранён) + `append(style=...)` из ORION_UI_COLORS, только
  ИЗВЕСТНЫЕ состояния красятся (незнакомый статус — plain, не гадаем);
  header на `_OK/_WARN/_CRIT/_ACTIVE` из палитры; лейблы focus-кнопок —
  константы COCKPIT_FOCUS_*_LABEL (один владелец, регрессия невозможна по
  построению); F2 ставит титулы/подписи рамок паттерном cockpit; общий
  MFD-блок в `orion_v.tcss` (по-парные id-селекторы), в экранах — только
  специфика (`min-height` у cockpit-main); margin правых экранов зеркален;
  стрелки сведены к ← → / ▲ ▼.

## Impact Metric

- Метрика: пины `tests/unit/test_ui_p2p3.py` (6) + живой Pilot-чек стилей.
- Baseline: рейл без спанов; header с ANSI; «Panel ▲» в исходнике; ASCII-стрелки.
- Target/Actual: **6 passed** + Pilot-чек «дедуп-стили применились, титулы
  стоят, margin зеркален, кнопка русская» PASS на ОБОИХ экранах; смежные
  пины header (4) и action_bar (5) адаптированы синхронно (R1) и зелёные;
  полный `tests/unit` **0 FAILED**; ruff-дифф чист (4 E501 pre-existing).

## Scope / Non-goals

- In scope: action_bar.py, header.py, cockpit.py, systems.py, orion_v.tcss,
  пины.
- NOT-FOUND (ревью, без изменений): «приглушение подсказки» — командная
  строка/консоль-стрип уже `$text-muted`; `#orionv-help` — активный фидбек,
  не подсказка.
- Out of scope: B3 (вынос `LAST …` из рейла во вторую строку — изменение
  раскладки, отдельное решение); полная классовая переработка MFD-CSS
  (селекторы сознательно оставлены по id — тождественность поведения
  доказуема, спецификация не менялась).

## Canon links

- Priority board (canonical): `~/MEMORI/ACTIVE_TASKS_QIKI_DTMP.md`
- Related docs/code:
  - `TASKS/TASK_20260709_post_review_polish.md` (P1-пакет, источник ярлыков)
  - DISPLAY_CANON №6 (страница — якорь в титуле рамки) — паттерн F2-фикса

## Plan (steps)

1) Свежая верификация ярлыков субагентом (6 CONFIRMED / 1 NOT-FOUND / +3). [сделано]
2) Фиксы + вынос тестируемых владельцев (rail-функция, лейблы-константы). [сделано]
3) Пины + синхронная адаптация смежных (header 4, action_bar 1) + Pilot-чек
   + живой рестарт консоли. [сделано]
4) Досье + гейты + коммит + main. [этот шаг]

## Definition of Done (DoD)

- [x] Docker-first checks passed (commands + outputs recorded)
- [x] Docs updated per `DOCUMENTATION_UPDATE_PROTOCOL.md` (это досье)
- [x] Операционный сценарий воспроизводится по `Reproduction Command`
- [x] Есть измеримый `Impact Metric` (baseline -> actual)
- [ ] Repo clean — после коммита среза

## Evidence (commands → output)

1. Пины: test_ui_p2p3 6 passed; полный tests/unit 0 FAILED (после R1-адаптации
   header/action_bar пинов на палитру/глифы).
2. Живой Pilot-чек стилей после CSS-дедупа (in-container):

```
[check] F1: tcss-стили живы, margin зеркален (лев (0,1,0,0) / прав (0,0,0,1)), кнопка русская ✓
[check] F2: титулы рамок стоят, дедуп-стили применились, margin зеркален ✓
[check] CSS-дедуп + титулы PASS
```

3. Консоль оператора перезапущена на новый код, вью w1:pW живая (рейл цел,
   свежий старт).
