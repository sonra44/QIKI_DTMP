# КАНОН ОТОБРАЖЕНИЯ — консоль оператора ORION V

Статус: ПРОЕКТ (формируется построчным проходом с оператором)
Дата: 2026-07-04
Метод: консилиум 4 перспектив — геймдизайн (референсы Duskers/Hacknet/Objects in Space),
лор-хранитель (SECTOR_TERTA/лор оператора), эргономика HMI (Boeing EICAS/Airbus/Союз/
Буран/НАСА MCC/ГОСТ Р 52303), страж честности (ADR-0014/0015/0016, §19).
Подчинён: QIKI Body v0.2.2, ADR-0014..0016, SECTOR_TERTA_CANON.

---

## 1. ЯЗЫКОВАЯ РАМКА (лор-фундамент)

Терминал ORION V — внутримирово **фасад станции обслуживания**, которая считает
оператора «возможно когнитивной эмерджентной сущностью» и потому генерирует
**семантический слой на русском**; **машинные факты-состояния** (RUN/OK/WARN/NODATA,
severity, reason-codes) — непереводимые коды.

- Русская проза = интерпретация станции для оператора.
- Латинские коды = факты машины. НЕ переводятся (уже действующее правило, теперь
  с лор-обоснованием).
- Станция не может знать внутреннее состояние оператора (эмоции, «устал») — только
  наблюдаемые сигналы (частота команд, молчание канала).

## 2. ЕДИНЫЙ ТЕСТ СТРОКИ (сводка четырёх формул)

**T0 — ШКАЛА ЗНАЧИМОСТИ ДЛЯ ОПЕРАТОРА (ведущий тест, оператор 2026-07-04).**
Прежде остальных тестов строка получает уровень значимости:

| Уровень | Признак | Право на место |
|---|---|---|
| ВИТАЛЬНАЯ | влияет на немедленные решения; взгляд каждые 3–5 сек | постоянная строка |
| ОПЕРАТИВНАЯ | нужна в контексте текущего действия | появляется по контексту |
| СПРАВОЧНАЯ | планирование; меняется редко | MFD-страница/tooltip/F-экран, НЕ постоянная строка |
| НУЛЕВАЯ | не влияет ни на одно решение | убрать |
| ВРЕДНАЯ | дезинформирует | убрать немедленно |

Далее строка живёт на экране, если проходит ВСЕ пункты:

| # | Тест | Голос |
|---|------|-------|
| T1 | Отвечает ровно на ОДИН из 5 вопросов оператора (что с ботом / что вокруг / что делает QIKI / что решать / что потеряю) | геймдизайн |
| T2 | Каждое ЗНАЧЕНИЕ имеет источник (телеметрия/расчёт/событие) или помечено `[TGT]`/NODATA; оформление (титул, метка, цвет) источника не требует | честность |
| T3 | Состояние мира несёт freshness (код/цвет/tooltip-возраст) | честность |
| T4 | ACK ≠ эффект; UI-состояние ≠ физическое состояние | честность (ADR-0015) |
| T5 | Один владелец факта (single-owner); дубль = merge или убрать | честность (ADR-0016) |
| T6 | Первичная строка = коды+цвет; проза → tooltip/F-экран; ≤ 70 символов; один тезис на строку | эргономика |
| T7 | Dark cockpit: при норме строка гаснет/сворачивается; постоянное место — только у критики и жизненных кодов | эргономика |
| T8 | Лор-чистота: понятен «кто говорит» (станция/QIKI), язык соответствует типу данных, детерминизм, видимая причинность, в пределах компетенции станции | лор |
| T9 | Мета-слой симуляции (пауза мира) визуально/семантически отделён от диегезиса | все |

Вердикты: PASS → живёт · 1-2 провала → переработать/tooltip · провал T1/T2 → убрать.

## 3. ПРАВИЛА ЗОН И ТИТУЛОВ

1. Зона имеет **ОДИН титул** — в верхней кромке рамки, **кириллицей** (практика
   Союз/Буран; титул = оформление, source не нужен). Имя спеки в доках остаётся
   английским (MISSION_CONTROL_STRIP — это ID документа, не показ).
2. **Нижние подписи рамок убрать** (двойное имя зоны — на реальных пультах не бывает;
   назначение зоны → tooltip/F9-глоссарий).
3. **Бренд системы не живёт в строке данных** — место бренда: заголовок консоли
   (уровень выше рамок) или меню/инструктаж.
4. **Имя экрана** — якорь `[F1]` в титуле зоны ИЛИ подсветка активной кнопки навбара;
   не отдельная строка.
5. Иерархия сверху вниз: живость канала → жизненные показатели → мир/тело →
   голос QIKI/решения → журнал/ввод. Красное всегда выше зелёного.

## 4. РЕЕСТР ПОСТРОЧНЫХ РЕШЕНИЙ

Заполняется построчным проходом. Формат: строка → решение → дата → кто утвердил.

| № | Элемент | Решение | Статус |
|---|---------|---------|--------|
| 1 | Титул Z1 + строка `ORION V \| F1 Кокпит` + подпись рамки | Титул `[F1] КОНТУР МИССИИ` (якорь = текущий экран, динамич.); ORION V → заголовок консоли над рамками («ORION V — терминал оператора»); строка бренда из стрипа УБРАНА (стрип = только коды, разделитель `·`); нижние именные подписи рамок убраны ВЕЗДЕ (единое правило; имена остальных зон — при их строках; хинт-подпись ВВОД — при её строке) | РЕШЕНО 2026-07-04 (оператор) |
| 2 | `МИР RUN · СВЯЗЬ OK · АКТУАЛ OK · СЕНС WARN` | МИР → **РЕПЛИКА** (лор: оператор видит локальную репликацию мира станцией; PAUSE = заморозка реплики, не вселенной; коды RUN/PAUSE/STOP/REPLAY/WAIT без изменений; tooltip несёт лор-пояснение). СВЯЗЬ/АКТУАЛ/СЕНС/УПР подтверждены как есть (ADR-0016) | РЕШЕНО 2026-07-04 (оператор) |
| 3 | Титул зоны `SAFETY & HEALTH STRIP` + строка `ALRT C0 W0 A0 · SAFE OK` + пустая строка overlay | Титул → **СОСТОЯНИЕ БОРТА** (имя оператора); пустая строка nominal-overlay **схлопнута** (dark cockpit: виджет display=False без живого алерта, появляется с алертом); метки `ALRT/SAFE` подтверждены латинскими кодами стрип-канона (расшифровка → tooltip/F9) | РЕШЕНО 2026-07-04 (оператор) |
| 4 | Ряд чипов подсистем | Чип = **метка + код + ОДИН якорь** всегда (`▸ PWR OK 100% ~262м` — PWR с двойным якорем SoC%+время; THRM `°`, PROP/HULL/CPU `%`, QIKI короткий статус-текст); **проза → tooltip** (сводка + клик-переход); один ряд без переносов | РЕШЕНО 2026-07-04 (оператор) |
| 5 | Строка MFD-статуса `РЕЖИМ: … \| КОРПУС: … \| УЛИКИ: … \| ИСТОЧНИК: …` | **УБРАНА ЦЕЛИКОМ** (T0: РЕЖИМ=нулевая-тройной дубль, КОРПУС/модули=справочная-живёт в правом MFD, УЛИКИ=нулевая на постоянке, ИСТОЧНИК=ВРЕДНАЯ — константа дезинформировала при живой телеметрии). История: окаменевший приёмочный heartbeat evidence-трека (780857c). Блок схлопывается; body/physics/power сводки показываются только когда правый MFD не «systems». `render_status_strip` удалён | РЕШЕНО 2026-07-04 (оператор) |
| 6 | Зона MFD (шапки панелей) | Титулы рамок **динамические**: `ЛЕВЫЙ MFD · <СТРАНИЦА>` / `ПРАВЫЙ MFD · <СТРАНИЦА>` (якорь страницы, меняется при `[`/`]`); внутренние строки-дубли («ЛЕВЫЙ MFD / РАДАР», секционный «Радар / Обстановка») **убраны** (T0: нулевая, четверной дубль) — контент сразу; хинты честности («фактов не выдумываем», «станция улик…») → **tooltip рамки** (T0: справочная); нижние подписи всех рамок кокпита убраны (правило №1) | РЕШЕНО 2026-07-04 (оператор) |
| 7 | Зона SOFTKEYS / ПЕРЕХОДЫ | Титул → **ПЕРЕХОДЫ**; дедуп рядов 1-2: `Питание/Тепло/Связь`-переходы **убраны** (T5: владелец — кликабельные чипы СОСТОЯНИЯ БОРТА, тот же роутинг `_route_metric_action`), уникальные `Маршрут · Стыковка · Инциденты · Процедуры` одним рядом; ряд петли (Предпросмотр/Применить) **отложен в трек G-D** «контекстные действия сцены»; ряд Panel▲/Help/Panel▼ без изменений | РЕШЕНО 2026-07-04 (оператор) |
| 8 | Зона QIKI / ОПЕРАТОР (панель Ф1-цикла + QIKI/Решение + Оператор/Действие + Процесс/Контур) | 8а: «Ф1 ЖИВОЙ ЦИКЛ»→**Ф1 ЦИКЛ** с влитым фокусом (`…\| действие: X (n/5) \| панель: Y`); СЕНСОРЫ/КОМАНДА-константы и acceptance-строки (`ПАНЕЛИ 6/6`+галки+`СОБЫТИЯ:`) **убраны** из loop-рендера (машинная проверка живёт в panel-VM/тестах); «цикл: снимок→…» только при Help·ON; 8б: строка ФОКУС **слита** в Ф1 ЦИКЛ; КЛАВИШИ/СПРАВКА/ПАЛИТРА/ПОДСКАЗКИ (обучалка + softkey-чипы) только при Help·ON, при OFF одна строка `H — справка \| Ctrl+P — палитра`; ПРОЦЕСС: PROCESS+FOCUS **сжаты в одну строку** `ПРОЦЕСС \| <state> \| <detail> · цель <focus>`; секция Процесс/Контур **поднята** сразу после Ф1 (max-height зоны 16→18 — иначе за кромкой при любом Help); словарь: «команда кораблю» → **«команда боту»** (канон 01_BODY_CANON §3: машинное тело, не корабль); 8в (голос QIKI, G-C): диалоговая лента `QIKI ▸ <HH:MM:SSZ> <ACK\|REJECT\|INFO> \| <текст ru>` (последние 3, новейшая сверху; ACK=allowed, REJECT=blocked/unsafe/deferred — команда не исполняется, INFO=реплика без арбитража; текст только из ответа: reply→legality.reason→error, без выдумок) сразу под головным рядом QIKI; сырые ряды LEGALITY/TRUST **убраны** из компакта → коды новейшей реплики в tooltip рамки (`LEGALITY <status> [domain] <code> · TRUST <state> conf=…`); PENDING/EFFECT/NEXT сохранены (G1 §3: consequence без silent failure); verbose [QIKI LOOP] не тронут; попутный runtime-фикс: подписки NATS изолированы (провал tracks-durable второй консоли больше не хоронит events/control/qiki-ответы) | РЕШЕНО 2026-07-04 (оператор) |
| 9 | Зона ACTION RAIL (нав-ряд + CMD-подсказка + кнопки инцидентов/страниц) | Малая уборка (F5-док §8.5): кнопка **«F5 QIKI»** добавлена в нав-ряд (долг M1 — биндинг f5+LEVEL_META были, кнопки не было; ведёт в существующий `show_level`, нового execute-пути нет); CMD-подсказка **без перечня экранов** — `'/' ':' — команда` (перечень был рассинхронен: без F5/F8; экраны показывают сами кнопки рейла; «H — справка» не упоминаем — H живёт только на F1); мёртвые кнопки инцидентов (`Инц ←/→`, `Подтв.`, `Снять`) → **show-when**: видимы только при выбранном инциденте, не торчат серыми (пагинация `< Стр / Стр >` как была — по контексту F3/F6); техдолг: снесён мёртвый англ. дубль `_action_rail_hint` (0 вызовов, дублировал русский `_hotkey_context`) | РЕШЕНО 2026-07-05 (оператор, приоритет «косметика и техдолг») |
| 9а | ACTION RAIL: органы времени и процедуры | **«⏸/▶ Мир»** — игровая (активная) пауза: постоянный операторский орган (виден всегда, disabled только в replay); label от живого `sim_state.paused`; жмёт `sim.pause`/`sim.start` на шину, ACK ≠ effect (эффект подтверждает чип РЕПЛИКА); команды `пауза`/`старт`. Активная пауза: консоль живёт (диалог/улики/одобрения), время процедур честно стоит (`WORLD_PAUSED`, ADR-0020). **«⏸/▶ Установка»** — show-when: только при активной процедуре установки | РЕШЕНО 2026-07-06 (оператор: «у оператора должна быть возможность останавливать время») |
| … | | | |

## 4а. ЦВЕТ (начало цветового канона)

- Рамка зоны = ОФОРМЛЕНИЕ: цвет-акцент допустим, но severity живёт только на
  кодах/чипах (рамка не «вечно-зелёная»).
- Верхняя «палуба» пульта (КОНТУР МИССИИ + СОСТОЯНИЕ БОРТА): **янтарный акцент
  `#d6a65b` 60%** (решение оператора 2026-07-04; фирменный тёплый акцент консоли).
- Остальные зоны — нейтральный `$surface-lighten-1 30%` до их строк прохода.
- Severity-цвета кодов: green/yellow/red/cyan(REPLAY) — без изменений (ADR-0016).

## 5. ОТКРЫТЫЕ РАЗВИЛКИ КОНСИЛИУМА (решает оператор)

- **ORION V — чьё имя?** Лор-хранитель: имя станции-терминала (V поколение;
  «культурная ссылка», подобранная станцией под сигнатуру оператора) — лор-чисто.
  Геймдизайнер: считать именем бота и перенести в identity-строку. Требует решения:
  ORION V = терминал/станция (рекомендация) или бот.
- ~~«МИР» мета-проблема~~ — РЕШЕНО (строка №2): лор-вариант «РЕПЛИКА».
- Титулы зон — итоговый словарь (КОНТУР МИССИИ / ЭШЕЛОН БЕЗОПАСНОСТИ / …) — по мере
  прохода.
