[pytest]
addopts = -q -m "not integration"
cache_dir = /tmp/pytest_cache
testpaths = tests
pythonpath = src generated
norecursedirs = UP
markers =
    integration: marks tests that require external services (run with `-o addopts='' -m integration`)
